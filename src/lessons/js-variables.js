// You can add javascript variables into your html by placing them
// between curly braces

export const JSVariables = () => {
  const message = "Hello";

  return (
    <div>
      <h1>{message}</h1>
    </div>
  );
};
